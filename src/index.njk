---
layout: layouts/base.njk
title: Home
---

<!-- Hero Section -->
<section class="min-h-[85vh] sm:min-h-[90vh] flex items-center">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-16 sm:py-24 text-center">
    <h1 class="font-sans font-light tracking-tighter text-4xl sm:text-5xl md:text-6xl lg:text-7xl mb-6 sm:mb-8 text-gray-900" style="line-height: 0.95;">
      Venture capital for critical systems
    </h1>

    <p class="text-lg sm:text-xl text-gray-500 max-w-3xl mx-auto mb-10 sm:mb-14 leading-relaxed">
      We are a family office investing in founders reshaping how we<br class="hidden sm:block">
      <span class="verb verb-ai">produce&nbsp;intelligence</span>, <span class="verb verb-crypto">move&nbsp;value</span>, <span class="verb verb-defense">provide&nbsp;security</span>, <span class="verb verb-health">give&nbsp;life</span>, and <span class="verb verb-community">bring&nbsp;joy</span>.
    </p>

    <!-- Sector cards -->
    <div class="flex flex-wrap justify-center gap-3">
      {% for sector in site.sectors %}
      {% if not sector.deemphasized %}
      <span class="px-4 py-2 rounded-lg text-sm font-medium transition-all hover:scale-105" style="background-color: {{ sector.color }}15; color: {{ sector.color }}; border: 1px solid {{ sector.color }}30;">
        {{ sector.name }}
      </span>
      {% endif %}
      {% endfor %}
    </div>
  </div>
</section>

